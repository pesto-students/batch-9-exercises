
const fs = require('fs');
const path = require('path');

const indexHtml = fs.readFileSync(path.resolve(__dirname, './index.html'), 'utf8');
const picturesHtml = fs.readFileSync(path.resolve(__dirname, './pictures.html'), 'utf8');
function iframe() {
  return html;
}
export {
  navigationMenuStart,
};
